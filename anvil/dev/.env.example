# anvil/dev/.env.example — copy to anvil/dev/.env (gitignored).
# Dev-safe defaults; never use in staging or production.

MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=dglab_dev
MYSQL_USER=dglab
MYSQL_PASSWORD=dglab
