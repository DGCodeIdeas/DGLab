# ARCHIVED — read-only provenance

**This directory is archived.** It is preserved for provenance only and is **not** part of the active
architecture.

- **Canonical source of truth:** `../../Architecture/` (see `../../Architecture/INDEX.md`).
- **Status:** Do not use for planning. This evaluation scores a **Core tier that no longer exists**
  (Finding 2) — its ID set and component names predate the renumbering in `Architecture/INDEX.md` §2.1.
  Every figure here is a dated snapshot and must be re-verified before use (governance Rule 3).

See `Architecture/INDEX.md` §2.1 for the canonical Core mapping.
