# ARCHIVED — read-only provenance

**This directory is archived.** It is preserved for provenance only and is **not** part of the active
architecture.

- **Canonical source of truth:** `../Architecture/` (see `../Architecture/INDEX.md`).
- **Status:** Do not edit, do not build from. This is **Vision A** code (the CMS-Studio monolith
  rebuild), superseded by the polyrepo `Architecture/` tree. Use `orchestrator/`, `packages/core/*`, and
  the Hub service repositories instead.

See `Architecture/INDEX.md` §1 for the canonical-vs-archived map.
